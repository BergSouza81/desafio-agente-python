version: '3.8'

services:
  api:
    build: .
    container_name: desafio-agente-python
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped

